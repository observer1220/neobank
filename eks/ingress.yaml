apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: nginx
spec:
  controller: k8s.io/ingress-nginx
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: neo-bank-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: "api.neobank24.net"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: neo-bank-api-service
            port:
              number: 80
